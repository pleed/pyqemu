- besseres thread handling
- frueherer "entry point"
- blacklist statt white list (pagefilter fuer instrumentation)

Ideen:

	- beliebige eingabe -> hohe entropie
	- schleifen! DONE
	- gleiche groesse im ausgabepuffer (meisstens)
	- keine nutzung von api funktionen (syscalls)    DONE

	Blockchiffren
		- diffusion/konfusion -> eingabebits beeinflussen viele andere eingabebits zum erzeugen der ausgabebits
		- umkehrung mit gleicher funktion
		- datenaenderungsquotient

	Stromchiffren
		- input laenge egal
		- daten werden aus pseudo rng generiert und fliessen in ausgabedaten mit ein

	- operationen: substitution, xor, permutation    DONE
	- normalerweise relative straighter programmfluss, ausgefuehrte instruktionen/opcodes hoeher als normal (schleifen) DONE
	- wird mit einem puffer nicht mehrmals aufgerufen
	- wenige branches (schleifen, keine if/switch anweisungen)


	- identifikation von prng?


- sniffing (network card hooking)
-

www.cuckoobox.org
